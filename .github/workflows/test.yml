name: Fail Step with Error Message

on: [push]

jobs:
  fail_step:
    runs-on: ubuntu-latest

    steps:
    - name: Failing Step
      run: |
        echo "::error:: This step is intentionally failing with an error message."
        exit 1
